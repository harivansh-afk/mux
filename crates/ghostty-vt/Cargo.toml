[package]
name = "ghostty-vt"
version = "0.2.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Headless terminal state tracking via ghostty VT (Zig FFI shim)"

[lints]
workspace = true

[dependencies]
serde.workspace = true

[dev-dependencies]
nix = { workspace = true, features = ["term", "process", "fs"] }
