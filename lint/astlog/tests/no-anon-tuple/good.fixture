fn pair() -> (u32, u32) {
    (0, 0)
}

fn expr() {
    let x = (1, 2, 3);
    let _ = x;
}

fn pat((a, b, c): (u8, u8)) {
    let _ = (a, b, c);
}

struct Triple {
    name: String,
    count: u8,
    live: bool,
}
